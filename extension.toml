id = "mise"
name = "mise"
version = "0.1.0"
schema_version = 1
authors = ["You"]
description = "mise support for Zed (dev-tools, tasks, and environment variables)"

[lib]
kind = "Rust"
version = "0.7.0"

snippets = ["./snippets/toml-tasks", "./snippets/shell-tasks"]
